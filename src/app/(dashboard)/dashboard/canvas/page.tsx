/**
 * Canvas List Page
 *
 * Server component for the flows list.
 * Displays all agent flows with create, duplicate, and delete actions.
 *
 * @module app/(dashboard)/dashboard/canvas/page
 */

import { CanvasListContent } from "@/components/canvas/canvas-list-content";

export default function CanvasPage() {
  return <CanvasListContent />;
}
